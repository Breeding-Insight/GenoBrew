% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mk_select.R
\name{plot_marker_positions}
\alias{plot_marker_positions}
\title{Plot marker positions across the genome}
\usage{
plot_marker_positions(
  marker_stats,
  colour_by = NULL,
  point_size = 0.3,
  bar_colour = "#d9d9d9",
  marker_colour = "#2166ac",
  bar_height = 0.5,
  gradient_colours = c("#2166ac", "#fc8d59", "#b2182b"),
  ploidy = 2L,
  interactive = FALSE
)
}
\arguments{
\item{marker_stats}{A data.frame of per-marker statistics, as produced by get_stats_df(). Must contain columns CHR, Position, and MarkerID. Additional columns (e.g., MAF, MeanDP, Pct_Missing, Pct_Het, In_Repeats, Pct_CNV_diff_ploidy) enable additional colour_by options.}

\item{colour_by}{Character. One of NULL (flat blue), "missing", "depth", "heterozygosity", "MAF", "repeated", or "CNV". Only available if the corresponding column is present in marker_stats.}

\item{point_size}{Numeric. Line width of the marker tick (default 0.3).}

\item{bar_colour}{Character. Fill colour for chromosome bars (default "#d9d9d9").}

\item{marker_colour}{Character. Flat tick colour when colour_by = NULL (default "#2166ac").}

\item{bar_height}{Numeric. Height of each chromosome bar (default 0.5).}

\item{gradient_colours}{Character vector of colours for the gradient scale. Defaults to a blue-orange-red palette (c("#2166ac", "#fc8d59", "#b2182b")).}

\item{ploidy}{Integer. Reference ploidy for CNV legend (default 2).}

\item{interactive}{Logical. If TRUE, adds a tooltip column for interactive plotting (default FALSE).}
}
\value{
A ggplot object (or data.frame with tooltip column if interactive = TRUE).
}
\description{
Creates a horizontal plot showing marker positions per chromosome. Each chromosome is drawn as a grey bar and each marker is a vertical tick. Ticks can optionally be coloured by a per-marker statistic derived from the marker statistics data frame.
}
\examples{
\dontrun{
stats <- get_stats_df(vcf, bed_data, win_data, dist_ploidy)
plot_marker_positions(stats)
plot_marker_positions(stats, colour_by = "missing")
plot_marker_positions(stats, colour_by = "MAF")
}
}
